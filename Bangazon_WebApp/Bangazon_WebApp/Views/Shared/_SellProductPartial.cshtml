@using Microsoft.AspNetCore.Identity
@using Bangazon_WebApp.Models

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
    @*Author: Ray  Purpose: This template renderes when the user is authenticated and shows the Sell a product Affordance in the nav bar*@
@if (SignInManager.IsSignedIn(User))
{
<ul class="nav navbar-nav">
    <li><a asp-area="" asp-controller="Products" asp-action="Create">Sell A Product</a></li>
</ul>
}
else
{
   
}